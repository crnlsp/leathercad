# zizmor, the workflow security linter run by CI's `workflows` job.
# https://docs.zizmor.sh/configuration/
rules:
  unpinned-uses:
    config:
      policies:
        # Every action, first-party ones included, pinned to a commit. A tag
        # can be moved to point at different code; a commit cannot.
        '*': hash-pin
