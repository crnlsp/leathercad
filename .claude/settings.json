{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); p=\"$CLAUDE_PROJECT_DIR/node_modules/.bin/prettier\"; case \"$f\" in *.ts|*.tsx|*.js|*.mjs|*.cjs|*.json|*.css|*.yaml|*.yml) [ -f \"$f\" ] && [ -x \"$p\" ] && \"$p\" --write --ignore-unknown \"$f\" >/dev/null 2>&1 ;; esac; exit 0"
          }
        ]
      }
    ]
  }
}
