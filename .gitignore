# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
out/
build/
# Except the desktop app's build resources: its icons, which are source and are
# committed (8.5a, `pnpm icons:generate`). electron-builder reads them there.
!apps/desktop/build/
release/
*.tsbuildinfo
.vite/

# Test output
coverage/
test-results/
playwright-report/
blob-report/
**/__snapshots__/*.actual.*
**/*.actual.png

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Local config and secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Claude Code local overrides (project settings ARE committed)
.claude/settings.local.json

# Mutation testing (pnpm test:mutation)
reports/
.stryker-tmp/

# Packaged app (pnpm package)
apps/desktop/release/
stryker-setup-*.js
