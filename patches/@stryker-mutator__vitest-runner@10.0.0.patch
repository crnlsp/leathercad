diff --git a/dist/src/stryker-setup.js b/dist/src/stryker-setup.js
index aecfacbe47b285c272c47e54b3d66a716adfdc41..1733b3fc24ce5d8a2f680831d2e67124e5269a54 100644
--- a/dist/src/stryker-setup.js
+++ b/dist/src/stryker-setup.js
@@ -63,7 +63,9 @@ function collectTestName({ name, suite, }) {
         nameParts.unshift(currentSuite.name);
         currentSuite = currentSuite.suite;
     }
-    return nameParts.join(' ').trim();
+    // LeatherCAD patch, until stryker-js#6210 ships: Vitest 5 matches
+    // testNamePattern against the chain joined with ' > '.
+    return nameParts.filter(Boolean).join(' > ').trim();
 }
 function toRawTestId(test) {
     return `${test.file?.filepath ?? 'unknown.js'}#${collectTestName(test)}`;
diff --git a/dist/src/test-helpers.js b/dist/src/test-helpers.js
index 56cc786f30c524ae18ce87eef1a67fdf26b703c5..f129e9a902a9d37d869831f7a2553bfd05df22d3 100644
--- a/dist/src/test-helpers.js
+++ b/dist/src/test-helpers.js
@@ -8,7 +8,10 @@ export function collectTestName({ name, suite, }) {
         nameParts.unshift(currentSuite.name);
         currentSuite = currentSuite.suite;
     }
-    return nameParts.join(' ').trim();
+    // LeatherCAD patch, until stryker-js#6210 ships: Vitest 5 matches
+    // testNamePattern against the chain joined with ' > '. Must agree with
+    // collectTestName in stryker-setup.js, or coverage keys stop matching.
+    return nameParts.filter(Boolean).join(' > ').trim();
 }
 export function toRawTestId(test) {
     return `${test.file?.filepath ?? 'unknown.js'}#${collectTestName(test)}`;
